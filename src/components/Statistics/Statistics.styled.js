import styled from 'styled-components';

const Wrapper = styled.div`
  max-width: 400px;
  padding: 5px;
`;

const Text = styled.p`
  font-size: 18px;
`;

const Accent = styled.span`
  font-weight: 700;
`;

export { Wrapper, Text, Accent };
